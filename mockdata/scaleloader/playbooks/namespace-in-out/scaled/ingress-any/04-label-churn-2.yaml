action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "ingress-from-any-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    ingress-from-any: "yes"
    just: "some churn 2"
spec:
  serviceaccountname: demosa
  nodeName: anode
