action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-from-any-an-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "ingress-from-any-ns": "yes"
    just: "some churn 2"
spec:
  serviceAccountName: demosa
  nodeName: anode
