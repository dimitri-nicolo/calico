action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-any-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-to-any: "yes"
    just: "some churn"
spec:
  serviceaccountname: demosa
  nodeName: anode
