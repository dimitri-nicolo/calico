action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-any-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-to-any: "yes"
    just: "some churn 2"
spec:
  serviceaccountname: demosa
  nodeName: anode
