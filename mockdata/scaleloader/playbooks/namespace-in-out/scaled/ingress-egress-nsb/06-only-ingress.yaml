action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "ingress-egress-with-nsb-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-ingress-from-nsb": "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
