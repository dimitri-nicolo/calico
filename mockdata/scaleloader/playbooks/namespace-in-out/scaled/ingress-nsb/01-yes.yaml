action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "ingress-from-nsb-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-ingress-from-nsb": "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
