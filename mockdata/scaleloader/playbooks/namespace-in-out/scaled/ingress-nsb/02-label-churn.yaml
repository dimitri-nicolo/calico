action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-from-nsb-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-ingress-from-nsb": "yes"
    just: "some churn"
spec:
  serviceAccountName: demosa
  nodeName: anode
