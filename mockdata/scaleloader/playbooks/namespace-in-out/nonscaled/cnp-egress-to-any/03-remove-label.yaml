action: update
kind: NetworkPolicy
apiversion: projectcalico.org/v3
metadata:
  name: egress-to-any
  namespace: "{{.Namespace}}"
spec:
  selector: egress-to-any == 'yes'
  egress:
    - action: allow
      destination:
        selector: 'all()'
  types:
    - Egress
