action: init
kind: NetworkPolicy
apiversion: projectcalico.org/v3
metadata:
  name: egress-to-nsb
  namespace: "{{.Namespace}}"
spec:
  selector: egress-to-nsb == 'yes'
  egress:
    - action: allow
      destination:
        namespaceSelector: 'nsb-label == "{{.Namespace}}"'
  types:
    - Egress
