action: update
kind: NetworkPolicy
apiversion: projectcalico.org/v3
metadata:
  name: egress-to-nsb
  namespace: "{{.Namespace}}"
  labels:
    churner: "oh yeahhhh"
spec:
  selector: egress-to-nsb == 'yes'
  egress:
    - action: allow
      destination:
        namespaceSelector: 'nsb-label == "{{.Namespace}}"'
  types:
    - Egress
