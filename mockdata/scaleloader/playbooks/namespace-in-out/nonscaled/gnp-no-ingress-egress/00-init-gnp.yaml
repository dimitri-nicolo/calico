action: init
kind: GlobalNetworkPolicy
apiversion: projectcalico.org/v3
metadata:
  name: "{{.Namespace}}-no-ingress-or-egress"
  labels:
    churn-gnp: "leslie"
spec:
  selector: '{{.Namespace}}-no-ingress-or-egress == "yes"'
  ingress:
    - action: allow
      source:
        selector: non-existent == endpoint
  egress:
    - action: allow
      destination:
        selector: non-existent == endpoint
  types:
    - Ingress
    - Egress
