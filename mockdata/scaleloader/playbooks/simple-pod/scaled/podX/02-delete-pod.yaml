action: delete
kind: "Pod"
apiVersion: v1
metadata:
  name: "demopod-a-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    labela: "value for label a"
    labelb: "label b value"
spec:
  serviceAccountName: demosa
  nodeName: anode
