action: init
kind: NetworkPolicy
apiversion: projectcalico.org/v3
metadata:
  name: simple-pod-protected
  namespace: "{{.Namespace}}"
spec:
  selector: protected == 'yes'
  ingress:
    - action: allow
      source:
        selector: 'ingress == "non-existent"'
  egress:
    - action: allow
      destination:
        selector: 'egress == "non-existent"'
  types:
    - Ingress
    - Egress
