action: delete
kind: "Pod"
apiVersion: v1
metadata:
  name: "unprotected-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "egress"
spec:
  serviceAccountName: demosa
  nodeName: anode
