action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "unprotected-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "egress"
spec:
  serviceaccountname: demosa
  nodeName: anode
