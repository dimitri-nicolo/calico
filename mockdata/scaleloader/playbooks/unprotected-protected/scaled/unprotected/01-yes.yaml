action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "unprotected-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
