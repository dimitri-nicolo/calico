action: delete
kind: "Pod"
apiversion: v1
metadata:
  name: "protected-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
