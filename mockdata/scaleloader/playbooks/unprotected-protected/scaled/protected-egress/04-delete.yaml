action: delete
kind: "Pod"
apiversion: v1
metadata:
  name: "protected-egress-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "egress"
spec:
  serviceaccountname: demosa
  nodeName: anode
