action: delete
kind: "Pod"
apiversion: v1
metadata:
  name: "protected-ingress-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "ingress"
spec:
  serviceaccountname: demosa
  nodeName: anode
