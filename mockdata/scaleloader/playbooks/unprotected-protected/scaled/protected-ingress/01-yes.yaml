action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "protected-ingress-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
