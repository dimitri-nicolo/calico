action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-pub-with-net-set-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-no-ingress-or-egress": "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
