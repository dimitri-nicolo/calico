action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-from-pub-with-noselector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    ingress-with-noselector: "yes"
    just: "some churn"
spec:
  serviceAccountName: demosa
  nodeName: anode
