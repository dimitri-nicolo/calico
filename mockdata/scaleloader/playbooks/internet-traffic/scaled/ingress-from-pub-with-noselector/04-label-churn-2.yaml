action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-from-pub-with-noselector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    ingress-with-noselector: "yes"
    just: "some churn 2"
spec:
  serviceAccountName: demosa
  nodeName: anode
