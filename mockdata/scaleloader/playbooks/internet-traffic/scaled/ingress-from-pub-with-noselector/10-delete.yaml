action: delete
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-from-pub-with-noselector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    ingress-with-noselector: "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
