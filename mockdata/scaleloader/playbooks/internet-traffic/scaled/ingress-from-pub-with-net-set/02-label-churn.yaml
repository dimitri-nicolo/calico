action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "ingress-from-pub-with-net-set-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-ingress-with-net-set": "yes"
    just: "some churn"
spec:
  serviceaccountname: demosa
  nodeName: anode
