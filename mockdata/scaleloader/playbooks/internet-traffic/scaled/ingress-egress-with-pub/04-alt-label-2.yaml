action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "ingress-egress-with-pub-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-ingress-with-net-set": "yes"
    egress-with-address-selector: "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
