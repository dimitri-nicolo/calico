action: delete
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-pub-with-address-selector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-with-address-selector: "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
