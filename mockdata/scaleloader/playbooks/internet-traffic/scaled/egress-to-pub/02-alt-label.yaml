action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-pub-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-with-address-selector: "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
