action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-pub-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-egress-with-net-set": "yes"
spec:
  serviceaccountname: demosa
  nodeName: anode
