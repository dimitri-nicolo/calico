action: delete
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-with-address-selector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    ingress-with-address-selector: "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
