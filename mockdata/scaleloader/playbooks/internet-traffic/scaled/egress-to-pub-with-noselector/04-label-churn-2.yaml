action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "egress-to-pub-with-noselector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-with-noselector: "yes"
    just: "some churn 2"
spec:
  serviceaccountname: demosa
  nodeName: anode
