action: init
kind: HostEndpoint
apiVersion: projectcalico.org/v3
metadata:
  name: "{{.Namespace}}-egress-to-internet-hep"
  labels:
    "{{.Namespace}}-egress-with-net-set": "yes"
spec:
  expectedIPs:
    - "172.0.0.2/32"
