action: update
kind: GlobalNetworkPolicy
apiversion: projectcalico.org/v3
metadata:
  name: "{{.Namespace}}-ingress-with-net-set"
spec:
  selector: '{{.Namespace}}-ingress-with-net-set == "yes"'
  ingress:
    - action: allow
      source:
        selector: gnslabel == "{{.Namespace}}"
  types:
    - Ingress
