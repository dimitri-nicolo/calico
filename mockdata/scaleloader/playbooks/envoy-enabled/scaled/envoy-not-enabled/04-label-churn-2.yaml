action: update
kind: "Pod"
apiversion: v1
metadata:
  name: "envoy-not-enabled-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    protected: "yes"
    just: "some churn 2"
spec:
  serviceaccountname: demosa
  nodeName: anode
