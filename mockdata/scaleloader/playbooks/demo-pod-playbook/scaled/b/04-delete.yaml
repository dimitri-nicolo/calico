action: delete
kind: "Pod"
apiversion: v1
metadata:
  name: "demopod-b-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    labela: "podb value for label a"
    labelb: "podb label b value"
spec:
  serviceaccountname: demo-b-sa
  nodeName: anode
