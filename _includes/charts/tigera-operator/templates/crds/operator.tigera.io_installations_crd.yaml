{{- /*
This file serves as an adapter so that CRDs exist in the place
between OS and Private even though OS and Private are using different helm version
and thus would normally store the CRDs in different locations.
*/}}
{{ $.Files.Get "crds/operator.tigera.io_installations_crd.yaml" }}
