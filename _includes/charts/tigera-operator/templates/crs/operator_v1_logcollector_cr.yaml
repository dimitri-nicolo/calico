{{ if .Values.logCollector.enabled }}
{{ $spec := omit .Values.logCollector "enabled" }}

apiVersion: operator.tigera.io/v1
kind: LogCollector
metadata:
  name: tigera-secure
spec:
{{ $spec | toYaml | indent 2 }}

{{ end }}
