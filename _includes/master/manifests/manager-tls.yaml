# Provides the credentials for TLS between the browser and CNX-Manager proxy.
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: cnx-manager-tls
  namespace: kube-system
data:
{{ include "calico.manager.tls" . | indent 2 }}
