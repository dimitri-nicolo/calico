policy abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ

Policy "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ" applies to these endpoints:
  Workload endpoint host1/k8s/g2w1/eth0; selector "calico/k8s_ns == 'group2'"
  Workload endpoint host1/k8s/g2w2/eth0; selector "calico/k8s_ns == 'group2'"
  Workload endpoint host1/k8s/g2w3/eth0; selector "calico/k8s_ns == 'group2'"

Endpoints matching Policy "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ" rules:
  Workload endpoint host1/k8s/g1w1/eth0
    inbound rule 1 source match; selector "calico/k8s_ns in {'group1','group2'}"
    outbound rule 1 destination match; selector "calico/k8s_ns in {'group1','group2'}"
  Workload endpoint host1/k8s/g1w2/eth0
    inbound rule 1 source match; selector "calico/k8s_ns in {'group1','group2'}"
    outbound rule 1 destination match; selector "calico/k8s_ns in {'group1','group2'}"
  Workload endpoint host1/k8s/g1w3/eth0
    inbound rule 1 source match; selector "calico/k8s_ns in {'group1','group2'}"
    outbound rule 1 destination match; selector "calico/k8s_ns in {'group1','group2'}"
  Workload endpoint host1/k8s/g2w1/eth0
    inbound rule 1 source match; selector "calico/k8s_ns in {'group1','group2'}"
    outbound rule 1 destination match; selector "calico/k8s_ns in {'group1','group2'}"
  Workload endpoint host1/k8s/g2w2/eth0
    inbound rule 1 source match; selector "calico/k8s_ns in {'group1','group2'}"
    outbound rule 1 destination match; selector "calico/k8s_ns in {'group1','group2'}"
  Workload endpoint host1/k8s/g2w3/eth0
    inbound rule 1 source match; selector "calico/k8s_ns in {'group1','group2'}"
    outbound rule 1 destination match; selector "calico/k8s_ns in {'group1','group2'}"
