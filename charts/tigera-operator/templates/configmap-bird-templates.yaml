{{- if .Values.bgp }}
kind: ConfigMap
apiVersion: v1
metadata:
  name: bird-templates
  namespace: {{.Release.Namespace}}
data:
{{- range $key, $value := .Values.bgp }}
{{ $key | indent 2 }}: |
{{ $value | indent 4 }}
{{- end }}
{{- end }}
