{{ if .Values.applicationLayer.enabled }}
{{ $spec := omit .Values.applicationLayer "enabled" }}

apiVersion: operator.tigera.io/v1
kind: ApplicationLayer
metadata:
  name: tigera-secure
spec:
{{ $spec | toYaml | indent 2 }}

{{ end }}
