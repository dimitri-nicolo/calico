{{ if .Values.policyRecommendation.enabled }}
{{ $spec := omit .Values.policyRecommendation "enabled" }}

apiVersion: operator.tigera.io/v1
kind: PolicyRecommendation
metadata:
  name: tigera-secure
spec:
{{ $spec | toYaml | indent 2 }}

{{ end }}
