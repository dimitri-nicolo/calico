apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnx-voltron.fullname" . }}-test-connection"
  labels:
{{ include "cnx-voltron.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cnx-voltron.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
