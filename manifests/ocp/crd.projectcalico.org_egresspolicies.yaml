apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: egresspolicies.crd.projectcalico.org
spec:
  group: crd.projectcalico.org
  names:
    kind: EgressPolicy
    listKind: EgressPolicyList
    plural: egresspolicies
    singular: egresspolicy
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EgressPolicySpec contains the egress policy rules for each
              destination network
            properties:
              rules:
                description: The ordered set of Egress Policies to define how traffic
                  exit a cluster
                items:
                  description: EgressRule defines a route rule for a destinatio network
                  properties:
                    cidr:
                      type: string
                    description:
                      description: The description of the EgressPolicy rule
                      type: string
                    egressGateway:
                      description: Gateway specifies the egress gateway that should
                        be used for this EgressRule
                      properties:
                        maxNextHops:
                          description: MaxNextHops
                          type: integer
                        namespaceSelector:
                          description: NamespaceSelector
                          type: string
                        selector:
                          description: Selector
                          type: string
                      type: object
                  required:
                  - cidr
                  type: object
                type: array
            required:
            - rules
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

