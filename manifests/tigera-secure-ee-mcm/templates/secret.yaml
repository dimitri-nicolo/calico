{{- if not .Values.certs.create }}
apiVersion: v1
kind: Secret
metadata:
    name: {{ .Values.secret.name }}
    namespace: {{ .Values.namespace }}
type: Opaque
data:
{{ ( include "voltron.gen-certs" . ) | indent 2 }}
{{- end -}}
