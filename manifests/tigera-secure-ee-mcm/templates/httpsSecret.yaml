{{- if not .Values.certs.https.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.httpsSecret.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
{{ ( include "voltron.gen-https-certs" . ) | indent 2 }}
{{- end -}}
