{{- if not .Values.certs.tunnel.create }}
apiVersion: v1
kind: Secret
metadata:
    name: {{ .Values.tunnelSecret.name }}
    namespace: {{ .Values.namespace }}
type: Opaque
data:
{{ ( include "voltron.gen-tunnel-certs" . ) | indent 2 }}
{{- end -}}
