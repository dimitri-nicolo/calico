  <store>
    @type splunk_hec
    protocol "#{ENV['SPLUNK_DNS_PROTOCOL']}"
    hec_host "#{ENV['SPLUNK_DNS_HEC_HOST']}"
    hec_port "#{ENV['SPLUNK_DNS_HEC_PORT']}"
    hec_token "#{ENV['SPLUNK_DNS_HEC_TOKEN']}"
    index "#{ENV['SPLUNK_DNS_INDEX']}"
    sourcetype "#{ENV['SPLUNK_DNS_SOURCETYPE']}"
    source "#{ENV['SPLUNK_DNS_SOURCE']}"
    <format>
      @type json
    </format>
    <buffer>
      flush_mode interval
      flush_interval "#{ENV['SPLUNK_DNS_FLUSH_INTERVAL']}"
      flush_thread_count "#{ENV['SPLUNK_DNS_FLUSH_THREAD_COUNT']}"
    </buffer>
  </store>
