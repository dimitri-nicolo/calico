  <store>
    @type splunk_hec
    protocol "#{ENV['SPLUNK_FLOW_PROTOCOL']}"
    hec_host "#{ENV['SPLUNK_FLOW_HEC_HOST']}"
    hec_port "#{ENV['SPLUNK_FLOW_HEC_PORT']}"
    hec_token "#{ENV['SPLUNK_FLOW_HEC_TOKEN']}"
    index "#{ENV['SPLUNK_FLOW_INDEX']}"
    sourcetype "#{ENV['SPLUNK_FLOW_SOURCETYPE']}"
    source "#{ENV['SPLUNK_FLOW_SOURCE']}"
    <format>
      @type json
    </format>
    <buffer>
      flush_mode interval
      flush_interval "#{ENV['SPLUNK_FLOW_FLUSH_INTERVAL']}"
      flush_thread_count "#{ENV['SPLUNK_FLOW_FLUSH_THREAD_COUNT']}"
    </buffer>
  </store>
