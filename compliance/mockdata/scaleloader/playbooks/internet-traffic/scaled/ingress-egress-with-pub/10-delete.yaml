action: delete
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-egress-with-pub-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    ingress-with-noselector: "yes"
    "{{.Namespace}}-egress-with-net-set": "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
