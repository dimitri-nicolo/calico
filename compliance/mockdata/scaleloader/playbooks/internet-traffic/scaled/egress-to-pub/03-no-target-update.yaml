action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "egress-to-pub-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-no-ingress-or-egress": "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
