action: delete
kind: "Pod"
apiVersion: v1
metadata:
  name: "egress-to-pub-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-egress-with-net-set": "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
