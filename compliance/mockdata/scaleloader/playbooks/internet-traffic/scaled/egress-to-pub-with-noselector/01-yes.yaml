action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "egress-to-pub-with-noselector-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-with-noselector: "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
