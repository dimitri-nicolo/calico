kind: "Pod"
apiVersion: v1
metadata:
  name: "demopod-b-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    labelb: "podb label b value 2"
spec:
  serviceAccountName: demo-b-sa
  nodeName: anode
