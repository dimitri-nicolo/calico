action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "egress-to-nsb-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    egress-to-nsb: "yes"
spec:
  serviceAccountName: demosa
  nodeName: anode
