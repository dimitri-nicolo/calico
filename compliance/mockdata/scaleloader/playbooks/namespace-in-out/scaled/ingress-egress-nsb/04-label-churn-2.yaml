action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "ingress-egress-with-nsb-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-ingress-from-nsb": "yes"
    egress-to-nsb: "yes"
    just: "some churn 2"
spec:
  serviceAccountName: demosa
  nodeName: anode
