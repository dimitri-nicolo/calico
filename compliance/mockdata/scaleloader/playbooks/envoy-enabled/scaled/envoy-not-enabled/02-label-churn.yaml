action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "envoy-not-enabled-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    protected: "yes"
    just: "some churn"
spec:
  serviceAccountName: demosa
  nodeName: anode
