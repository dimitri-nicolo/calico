action: update
kind: "Pod"
apiVersion: v1
metadata:
  name: "protected-egress-pod-{{.ScaleId}}"
  namespace: "{{.Namespace}}"
  labels:
    "{{.Namespace}}-protected": "egress"
spec:
  serviceAccountName: demosa
  nodeName: anode
