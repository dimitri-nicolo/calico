action: init
kind: HostEndpoint
apiVersion: projectcalico.org/v3
metadata:
  name: "{{.Namespace}}-egress-protected-hep"
  labels:
    "{{.Namespace}}-protected": "egress"
spec:
  expectedIPs:
    - "172.0.0.2/32"
