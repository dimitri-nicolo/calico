apiVersion: calico.k8s.io/v1
kind: Policy
metadata:
  name: Tier1.my-first-policy
spec:
  selector: role == 'database'
  ingress:
  - action: allow
    protocol: tcp
    source:
      selector: role == 'frontend'
    destination:
      ports:
      - 6379
  egress:
  - action: allow
