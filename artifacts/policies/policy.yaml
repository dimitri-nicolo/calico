apiVersion: calico.tigera.io/v1
kind: NetworkPolicy
metadata:
  name: my-first-policy
spec:
  selector: role == 'database'
  ingress:
  - action: allow
    protocol: tcp
    source:
      selector: role == 'frontend'
    destination:
      ports:
      - 6379
  egress:
  - action: allow
