---
layout: null
---
# {{site.tseeprodname}} Version {{site.data.versions[page.version].first.title}}
# {{site.url}}/{{page.version}}/releases#{{site.data.versions[page.version].first.title}}

# The following contains k8s Secrets for use with {{site.tseeprodname}} Federation.
# For information on populating Secrets, see http://kubernetes.io/docs/user-guide/secrets/
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: tigera-federation-remotecluster
  namespace: kube-system
data:
  # Populate the data with any certificates, keys, kubeconfigs etc. that need to be referenced in the
  # configured RemoteClusterConfiguration resources.
